import { CertificationLevel, ExtensionCategory, ExtensionProcessType, ExtensionRiskLevel, ExtensionRuntimeType, ExtensionType, TenantIsolationMode } from './index.js';
import { DeveloperPlatform, SubmissionStatus } from './index.js';
const manifest={id:'com.dev.analytics',name:'Analytics',description:'Metadata analytics',publisherId:'dev-a',version:'1.0.0',type:ExtensionType.ServicePlugin,category:ExtensionCategory.AnalyticsModule,entrypoints:[{id:'svc',runtime:ExtensionRuntimeType.Container,module:'image',processType:ExtensionProcessType.Analytics,startupMode:'automatic' as const}],capabilities:[{capability:'analytics.metadata.read',reason:'Read approved metadata',required:true,riskLevel:ExtensionRiskLevel.Low}],supportedUBOSVersions:'v5.11',supportedEnvironments:['cloud'],tenantIsolationMode:TenantIsolationMode.MultiTenantIsolated,riskLevel:ExtensionRiskLevel.Low,dependencies:[],support:{contact:'support',responseTargetHours:12},license:{model:'subscription'},integrity:{signature:'sig',sha256:'sha256:abc',signedBy:'key'}};
const p=new DeveloperPlatform(); p.createOrganization({id:'dev-a',name:'Dev A',verified:false,suspended:false,createdAt:1}); try{p.createSigningKey({id:'key',developerId:'dev-a',publicKey:'pk',revoked:false,createdAt:1}); throw new Error('unverified key accepted');}catch(e){ if((e as Error).message==='unverified key accepted') throw e; } p.verifyOrganization('dev-a'); p.createSigningKey({id:'key',developerId:'dev-a',publicKey:'pk secret',revoked:false,createdAt:2}); const s=p.submit(manifest,{submissionId:'sub-a',packageReference:'pkg credential',requestedCertificationLevel:CertificationLevel.ProductionReady,submittedAt:3}); if(s.status!==SubmissionStatus.SecurityReview) throw new Error('submission status wrong'); p.certify('sub-a'); p.publish('sub-a'); if(p.metrics('dev-a').published!==1) throw new Error('publisher metrics wrong'); if(!p.snapshot().keys[0]?.publicKey.includes('[REDACTED]')) throw new Error('key redaction failed'); console.log('v5.11.9 developer-platform validation passed');
