import type { ProductionCommand, ProductionEvent, ProductionGraph } from '../production-graph.js';
import type { EngineSubsystem, ResourceLock } from './types.js';
import type { EngineValidationIssue } from './validation.js';

export type ProductionExecutionStatus = 'accepted' | 'rejected' | 'applied' | 'failed' | 'blocked' | 'dry_run';
export type Phase17CommandType = ProductionCommand['type'] | 'ADD_GRAPHICS_LAYER' | 'UPDATE_GRAPHICS_LAYER' | 'REMOVE_GRAPHICS_LAYER' | 'STAGE_GRAPHICS_PREVIEW' | 'TAKE_GRAPHICS_TO_PROGRAM' | 'ADD_MEDIA_ASSET' | 'STAGE_MEDIA_PREVIEW' | 'TAKE_MEDIA_TO_PROGRAM' | 'SET_OUTPUT_ROUTE' | 'ARM_AUTOMATION_CUE' | 'MARK_AUTOMATION_CUE_EXECUTED';
export interface ProductionExecutionRequest { id: string; command: ProductionCommand<Phase17CommandType & ProductionCommand['type']> | (Omit<ProductionCommand, 'type'> & { type: Phase17CommandType }); operatorId: string; source: 'operator' | 'automation' | 'ai' | 'replay' | string; timestamp: string; dryRun: boolean; metadata: Record<string, unknown>; }
export interface GraphMutation { id: string; type: 'set' | 'add' | 'update' | 'remove'; targetType: string; targetId: string; before: unknown; after: unknown; metadataOnly: true; }
export interface GraphMutationPlan { id: string; commandId: string; targetGraphRevision: number; mutations: GraphMutation[]; dependencies: string[]; locks: string[]; validation: { valid: boolean; issues: EngineValidationIssue[] }; reversible: boolean; }
export interface ExecutionPolicy { allowUnsafeCommands: false; requireLocks: boolean; requireKnownTargets: boolean; allowDryRun: boolean; allowReplay: boolean; }
export interface EngineTransactionRecord { id: string; commandId: string; operatorId: string; subsystem: EngineSubsystem; timestamp: string; status: ProductionExecutionStatus; graphRevisionBefore: number; graphRevisionAfter: number; reversible: boolean; warnings: string[]; errors: string[]; }
export interface EngineSnapshotRecord { id: string; programScene?: string; previewScene?: string; sources: Record<string, unknown>; graphicsMetadata: Record<string, unknown>; mediaMetadata: Record<string, unknown>; outputRoutes: Record<string, unknown>; automationState: Record<string, unknown>; collaborationState: Record<string, unknown>; distributionState: Record<string, unknown>; deviceState: Record<string, unknown>; revision: number; createdAt: string; containsRuntimeHandles: false; }
export interface ProductionExecutionResult { id: string; commandId: string; status: ProductionExecutionStatus; events: ProductionEvent[]; transaction?: EngineTransactionRecord; snapshot?: EngineSnapshotRecord; warnings: string[]; errors: string[]; graphRevisionBefore: number; graphRevisionAfter: number; containsRuntimeHandles: false; graph: ProductionGraph; plan?: GraphMutationPlan; }
export interface ReplayReconstructionResult { events: ProductionEvent[]; reconstructedSnapshot: EngineSnapshotRecord; graphRevision: number; warnings: string[]; errors: string[]; unsupportedEvents: ProductionEvent[]; }
export const defaultExecutionPolicy: ExecutionPolicy = { allowUnsafeCommands: false, requireLocks: true, requireKnownTargets: true, allowDryRun: true, allowReplay: true };
