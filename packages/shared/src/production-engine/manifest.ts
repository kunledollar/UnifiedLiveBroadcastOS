import { PRODUCTION_ENGINE_SCHEMA_VERSION, type DependencyGraph, type EngineManifest, type EngineState, type ProductionCommandType, type ProductionEventType, type ResourceType } from './types.js';
import { sanitizeEngineId } from './validation.js';
const now = '1970-01-01T00:00:00.000Z';
const base = (id:string,label:string,description:string)=>({id:sanitizeEngineId(id),schemaVersion:PRODUCTION_ENGINE_SCHEMA_VERSION,label,description,createdAt:now,updatedAt:now,containsRuntimeHandles:false as const});
const commands: ProductionCommandType[] = ['CUT','AUTO','TAKE','LOAD_SCENE','SHOW_GRAPHIC','HIDE_GRAPHIC','PLAY_MEDIA','STOP_MEDIA','START_RECORDING','STOP_RECORDING','ROUTE_OUTPUT','CHANGE_LAYOUT','EXECUTE_MACRO','ENABLE_OUTPUT','DISABLE_OUTPUT'];
const events: ProductionEventType[] = ['SceneLoaded','GraphicShown','MediaStarted','GuestJoined','OutputEnabled','AutomationStarted','RecordingStarted','TransitionFinished','ReplayLoaded','DeviceConnected','TimelineAdvanced'];
const resources: ResourceType[] = ['program_output','preview_output','audio_bus','graphic_layer','replay_channel','recording_encoder','media_player','camera_input'];
export function createProductionEngineManifest(): EngineManifest { const graph: DependencyGraph = {...base('engine-dependency-graph','Dependency Graph','Metadata-only production dependency graph.'), schedulingEnabled:false, nodes:['Scene','Graphics','Media','Replay','Outputs'].map((name,index)=>({...base(name,name,`${name} dependency node.`),dependencies:index? [sanitizeEngineId(['Scene','Graphics','Media','Replay','Outputs'][index-1]!)]:[],priority:index+1,executionOrder:index+1,locked:false,validationStatus:'unknown'})), edges:[['Scene','Graphics'],['Graphics','Media'],['Media','Replay'],['Replay','Outputs']].map(([from,to])=>({from:sanitizeEngineId(from!),to:sanitizeEngineId(to!),label:'metadata dependency'}))}; const state: EngineState = {...base('engine-state','Engine State','Safe default engine readiness metadata.'),status:'unavailable',commandBusStatus:'Unavailable',eventBusStatus:'Unavailable',queueStatus:'Unavailable',lockStatus:'Unavailable',timelineStatus:'Unavailable',conflictStatus:'Unavailable',runtimeConnected:false,productionGraphIntegrated:false}; return {...base('production-engine-manifest','Production Engine Manifest','Phase 16 metadata-only Production Engine foundation.'), commands: commands.map((type)=>({...base(`command-${type}`,type,`${type} command definition only.`),type,subsystem:'engine',payloadSchema:{targetId:'string'},safeDefaultPayload:{},dependencies:[],rollbackAvailable:false,metadataOnly:true})), events: events.map((type)=>({...base(`event-${type}`,type,`${type} event definition only.`),type,subsystem:'engine',payloadSchema:{sourceId:'string'},severity:'info',descriptiveOnly:true})), dependencyGraph:graph, resourceLocks:resources.map((resourceType)=>({...base(`lock-${resourceType}`,resourceType.replace(/_/g,' '),`${resourceType} ownership metadata.`),resourceType,owner:'Unavailable',state:'available',metadataOnly:true})), executionQueue:{...base('execution-queue','Execution Queue','Metadata-only queue. No runtime connected.'),state:'waiting',actions:[],blockedReason:'No runtime connected',executing:false}, timeline:{...base('engine-timeline','Engine Timeline','Timeline markers only. No playback engine.'),currentTime:'Unavailable',currentSegment:'Unavailable',upcomingSegment:'Unavailable',completedSegments:[],cueMarkers:[],transitionMarkers:[],breakpoints:[],playbackEnabled:false}, snapshots:[{...base('engine-snapshot','Engine Snapshot','Snapshot schema with unavailable metadata defaults.'),currentScene:'Unavailable',graphicsState:'Unavailable',mediaState:'Unavailable',replayState:'Unavailable',outputs:'Unavailable',audio:'Unavailable',automation:'Unavailable',timeline:'Unavailable',guests:'Unavailable',devices:'Unavailable',persistenceEnabled:false}], transactions:[], history:[], subscriptions:[], state}; }
