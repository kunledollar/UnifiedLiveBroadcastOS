import { CertificationLevel, ExtensionCategory, ExtensionProcessType, ExtensionRiskLevel, ExtensionRuntimeType, ExtensionType, TenantIsolationMode } from './index.js';
import { MarketplaceCatalog } from './index.js';
const manifest={id:'com.vendor.graphics',name:'Graphics Pack',description:'Lower thirds templates',publisherId:'pub',version:'1.0.0',type:ExtensionType.DeclarativePackage,category:ExtensionCategory.GraphicsPackage,entrypoints:[{id:'pack',runtime:ExtensionRuntimeType.Declarative,module:'manifest.json',processType:ExtensionProcessType.UI,startupMode:'manual' as const}],capabilities:[{capability:'graphics.prepare',reason:'Prepare approved graphics',required:true,riskLevel:ExtensionRiskLevel.Low}],supportedUBOSVersions:'v5.11',supportedEnvironments:['cloud'],tenantIsolationMode:TenantIsolationMode.MultiTenantIsolated,riskLevel:ExtensionRiskLevel.Low,dependencies:[],support:{contact:'support',responseTargetHours:24},license:{model:'paid'},integrity:{signature:'sig',sha256:'sha256:abc',signedBy:'key'}};
const cat=new MarketplaceCatalog(); cat.publish({id:'listing-a',manifest,publisherVerified:true,certificationLevel:CertificationLevel.Verified,pricingModel:'paid',supportContact:'support',publishedAt:1,suspended:false,dataHandling:'no token secret'}); cat.grantEntitlement({tenantId:'tenant-a',extensionId:manifest.id,status:'expired',expiresAt:2,offlineGraceUntil:10}); if(!cat.canInstall('tenant-a',manifest.id,5)) throw new Error('offline grace rejected'); cat.review({id:'rev',listingId:'listing-a',tenantId:'tenant-a',rating:5,text:'works with password hidden',versionUsed:'1.0.0',verifiedInstallation:true,createdAt:6}); cat.suspend('listing-a','credential compromise'); if(cat.search({text:'graphics'}).length!==0) throw new Error('suspended listing visible by default'); if(!cat.snapshot().reviews[0]?.text?.includes('[REDACTED]')) throw new Error('review not redacted'); console.log('v5.11.9 marketplace validation passed');
