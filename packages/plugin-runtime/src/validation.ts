import { ExtensionCategory, ExtensionProcessType, ExtensionRiskLevel, ExtensionRuntimeType, ExtensionType, PluginRestartPolicy, TenantIsolationMode } from './index.js';
import { PluginLifecycleState, PluginRuntime } from './index.js';
const manifest={id:'com.vendor.device',name:'Device',description:'Device driver',publisherId:'pub',version:'1.0.0',type:ExtensionType.DevicePlugin,category:ExtensionCategory.DeviceDriver,entrypoints:[{id:'driver',runtime:ExtensionRuntimeType.WebAssembly,module:'driver.wasm',processType:ExtensionProcessType.Device,startupMode:'automatic' as const}],capabilities:[{capability:'device.register',reason:'Register approved device',required:true,riskLevel:ExtensionRiskLevel.Medium}],supportedUBOSVersions:'v5.11',supportedEnvironments:['edge'],tenantIsolationMode:TenantIsolationMode.SingleTenantInstance,riskLevel:ExtensionRiskLevel.Medium,dependencies:[],support:{contact:'support',responseTargetHours:8},license:{model:'subscription'},integrity:{signature:'sig',sha256:'sha256:abc',signedBy:'key'}};
const rt=new PluginRuntime(); const inst=rt.install(manifest,{tenantId:'tenant-a',installedBy:'admin',approvedBy:'owner',capabilities:['device.register','production.program.change'],limits:{maximumMemoryMb:256,maximumCPUPercent:10,maximumThreads:4,maximumOpenFiles:16,networkPolicyId:'local-devices',restartPolicy:PluginRestartPolicy.LimitedRetries},at:1});
rt.enable(inst.identity.installationId,2); if(!rt.execute(inst.identity.installationId,'device.register',{tenantId:'tenant-a'})) throw new Error('capability denied'); if(rt.execute(inst.identity.installationId,'production.program.change',{tenantId:'tenant-a'})) throw new Error('undeclared capability granted'); for(let i=0;i<5;i++) rt.recordCrash(inst.identity.installationId,'token secret',3+i); if(rt.snapshot().installations[0]?.state!==PluginLifecycleState.Suspended) throw new Error('crash loop not suspended'); console.log('v5.11.9 plugin-runtime validation passed');
