# UBOS Architecture Notes

The scaffold separates product surfaces, integration contracts, and persistence so the production layer can support Edge Agent, OBS adapter, cloud renderer, or future compositor implementations without hard-coding a single vendor.
