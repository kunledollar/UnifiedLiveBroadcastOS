# UBOS Next Visual Validation

Browser validation is pending in this execution environment. Planned captures cover all nine workspaces at 1920×1080 plus Director, Audio, Solo at 1366×768; Monitor Wall at 2560×1440; Streaming at 3440×1440; and vertical, destination, guest, and graphics fixtures. The next browser-validation pass must record route, workspace, fixture, viewport, visible panels, overflow, console errors, layout errors, and runtime binding status.

Known limitation: the presentation prototype deliberately uses CSS monitor illustrations rather than runtime media, so it cannot certify production video behavior.
