# Output Metrics

The runtime collects metadata for uptime, frames sent, bytes sent, bandwidth, latency, errors, restarts, and recoveries.

Metrics are supplied by existing engines or adapters and are not derived from new media processing.
