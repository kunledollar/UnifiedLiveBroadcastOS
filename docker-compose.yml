services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: ubos
      POSTGRES_PASSWORD: ubos
      POSTGRES_DB: ubos
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
volumes:
  postgres_data:
