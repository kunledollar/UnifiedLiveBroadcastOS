{
  "name": "@ubos/desktop",
  "version": "1.0.0-rc.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/wait-for-web.mjs && cargo run --manifest-path src-tauri/Cargo.toml",
    "build": "cargo build --manifest-path src-tauri/Cargo.toml",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "desktop:demo": "pnpm dev"
  }
}
