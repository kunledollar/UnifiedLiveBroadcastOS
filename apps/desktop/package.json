{
  "name": "@ubos/desktop",
  "version": "1.0.0-rc.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @ubos/web dev",
    "build": "tsc -p tsconfig.json --noEmit",
    "build:native": "cargo build --manifest-path src-tauri/Cargo.toml",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "desktop:demo": "pnpm --filter @ubos/web dev"
  }
}
