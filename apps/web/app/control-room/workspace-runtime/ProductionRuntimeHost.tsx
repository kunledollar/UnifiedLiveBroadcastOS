'use client';
import {memo,useEffect,useState} from 'react';
const Monitor=memo(function Monitor({kind}:{kind:'program'|'preview'}){return <section className={`ubos-monitor ubos-monitor-${kind}`} data-testid={`ubos-${kind}-monitor`} data-monitor={kind}><header><b>{kind==='program'?'PROGRAM':'PREVIEW'}</b><span>{kind==='program'?'LIVE · Host Welcome':'NEXT · Product Overview'}</span></header><div><strong>{kind==='program'?'PROGRAM':'PREVIEW'}</strong><small>Persistent production monitor · 16:9</small></div></section>});
export function ProductionRuntimeHost(){const [clock,setClock]=useState('');useEffect(()=>{const tick=()=>setClock(new Date().toLocaleTimeString([], {hour:'2-digit',minute:'2-digit',second:'2-digit'}));tick();const id=window.setInterval(tick,1000);return()=>window.clearInterval(id)},[]);return <section className="ubos-monitor-host" aria-label="Program and Preview monitors" data-testid="production-runtime-host"><Monitor kind="program"/><Monitor kind="preview"/><aside className="ubos-runtime-state">LIVE · REC ● · STREAM ●<small>{clock}</small></aside></section>}
